{% layout "blog" %}
{% block content %}
	<article>
		<h1>Archive</h1>
		{% archive "blog" %}
		{% for item in archive.items %}
			<h3>{{ item.year }}</h3>
			<ul class="archive">
			{% for post in item.children %}
				<li><a href="{{ post.slug }}">{{ post.title }}</a></li>
			{% endfor %}
			</ul>
		{% endfor %}
	{% endarchive %}
	</article>
{% endblock %}